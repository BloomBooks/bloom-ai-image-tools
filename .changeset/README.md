# Changesets

This repo uses [Changesets](https://github.com/changesets/changesets) to manage releases. Run `vp run changeset` after making code changes to record the appropriate semver bump. Commit the generated markdown file and open a PR.
